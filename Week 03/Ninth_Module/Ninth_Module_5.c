#include <stdio.h>

int main() {
    /* int marks = 80;

    if (marks == 0) {
        printf("You got dabba");
    }
    if (marks >= 40) {
        printf("you have passed");
    }
    else { 
        printf("you have failed");
    } */

    /* int eng_marks = 0, math_marks = 0;

    if (eng_marks >= 40 && math_marks >= 40)
    {
        printf("you have passed");
    }
    else
    {
        printf("you have failed");
    } */

    /* int eng_marks = 55, math_marks = 39;

    if (eng_marks >= 40 || math_marks >= 40)
    {
        printf("you have passed at least one subject");
    }
    else
    {
        printf("you have failed");
    } */

    /* int eng_marks = 55, math_marks = 39;

    if ( !(eng_marks >= 40 || math_marks >= 40) )
    {
        printf("you have passed at least one subject");
    }
    else
    {
        printf("you have failed");
    } */

    /* int eng_marks = 55, math_marks = 39;

    if (eng_marks >= 40)
    {
        if (math_marks >= 40) {
            printf("Pass both");
        }
    }
    else
    {
        printf("you have failed");
    } */

    int eng_marks = 55, math_marks = 39;

    int num;

    return 0;
}

/*
Relational: >= <= > < != ==
Logical: && || !
    if (condition) {
        statements;
    }
    else {
        statements;
    }
*/
